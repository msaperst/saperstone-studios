{
    "name": "fatmax.com/saperstone-studios",
    "description": "Saperstone Studios website tooling needed for localized testing",
    "require-dev": {
        "phpunit/phpunit": "^7"
    },

    "scripts": {
        "unit-test": "vendor/phpunit/phpunit/phpunit tests/unit/ --log-junit reports/junit.xml --coverage-clover reports/clover.xml --coverage-html reports/html --whitelist src/"
    }
}
