; Do not advertise the PHP runtime/version in HTTP response headers.
expose_php = Off

; Never disclose runtime or startup diagnostics in HTTP responses.
display_errors = Off
display_startup_errors = Off

; Preserve diagnostics in the container logs for operators.
log_errors = On
error_log = /proc/self/fd/2
